# MASSIV — scroll polish report

## Проверено

- Production build: успешно.
- Скролл не блокируется и остаётся управляемым пользователем.
- Hero: серия из 8 быстрых прокруток дала строго монотонные кадры `0.52 → 1.04 → 1.57 → 2.10 → 2.62 → 3.15 → 3.67 → 4.19s`, без зависшего `seeking`.
- Последний кадр достигается: `5.031s / 5.041s` (`99.8%`, оставлена только техническая граница 0.01s).
- Текст следующей видеоглавы виден на 76% ещё до её фиксации.
- В консоли браузера нет ошибок.
- Мобильный viewport 375×812: горизонтального переполнения нет; sticky отключён.
- Все горячие scroll-пути используют `transform`, `opacity` или `video.currentTime`.
- Добавлен статический `prefers-reduced-motion` fallback.

## Режиссёрская проверка

- Фокус первого экрана сохраняется на дверном проёме и крупном заголовке.
- Глава не отпускается до достижения последнего кадра видео.
- Затемнение начинается до отпускания sticky-панели, поэтому чёрного провала между секциями нет.
- Русский текст остаётся HTML и доступен для выделения и экранных дикторов.
# Доработка переходов и пауз — 5 июля 2026

- Длинные проявления заменены коротким раскрытием следующей сцены внутри предыдущего кадра (24% высоты экрана).
- «Философия», «Проекты» и «Контакты» закрепляются безопасным CSS sticky — текст остаётся неподвижным для чтения без изменения DOM со стороны GSAP.
- Пустые нижние отступы текстовых сцен сокращены; градиентные хвосты уменьшены со 120 до 48 пикселей.
- Верхняя навигация Hero больше не гаснет при прокрутке; hover сохраняет полную видимость.
- Устранён конфликт React/GSAP `insertBefore`: удалены GSAP pin-spacer и перестановка узлов. Двойная перезагрузка после прокрутки проходит без runtime overlay.
- Production build: успешно.
